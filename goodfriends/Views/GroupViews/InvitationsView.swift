import SwiftUI

struct InvitationsView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world invitations!")
        }
        .padding()
    }
}

#Preview {
    InvitationsView()
}
