import SwiftUI

struct BalancesView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world balances!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
